Welcome to Jurassic Park, <%= @user.name %>

<h3>Create a new listing</h3>
<br>
<form action="/create">
  <button>Create new listing</button>
</form>
<br>
<h1> A list of all spaces: </h1>
<ul>
  <% @spaces.each do |space| %>
  <li>
    Name: <%= space.name %>, Description: <%= space.description %>, Price per day: <%= space.price %>, Available from: <%= space.available_from %>, Available to: <%= space.available_to %>
    <form action="/spaces/edit" method="post">
      <button type="submit" name="Edit" value="<%= space.id %>">Edit</button>
    </form>
  </li>
  <% end %>
</ul>
